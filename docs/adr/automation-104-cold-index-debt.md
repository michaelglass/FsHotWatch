# Cold index rows become a baseline by consuming verification debt

A real cold-check integration control failed after an earned full baseline: editing ColdA selected ColdA and unchanged ColdB. The retained daemon log showed ColdB rows classified as `RowsFromThisRun`, then diffed against nothing on the forced scan. Initial clean `Unknown, NoRows` extraction had selected `NothingHidden`, relying on a future full run, so it never called `MarkBaselineEstablished`.

For configured tests, consume that initial clean extraction through the existing `Diffable AgainstNothing` path. Added symbols become durable verification debt before the rows become a comparison baseline. This preserves the distinction between indexing and test evidence and avoids another ledger for later promotion. Analysis-only behavior stays unchanged. A228, A277 and A526 refusal behavior remains in the shared freshness policy.

The existing optimization that attaches a late BootScan cohort to an active full run must retain the first captured symbol revisions. A completion may retire those late candidates only when the actual run was full, its launch and current input identities match, and each current symbol revision equals the captured revision. A later edit remains owed even if the original bytes are restored. Ordinary launch-set retirement is unchanged.

Evidence: `/tmp/a104-cold-diagnosis4.log` reproduced filtered 2/2 instead of 1/2 with actual ColdB executions. `/tmp/a104-boot-revision-red2.log` had 73 passing and two failing controls: both a post-seal edit and edit-then-restore lost durable Lib.foo debt. `/tmp/a104-boot-revision-green.log` passed all 75, including the unchanged one-full-run positive. `/tmp/a104-cold-corrected.log` passed all four real command controls with unchanged assertions, including filtered 1/2 and changed failing bytes. The final full CI and integration gates remain separate requirements.

A separate foreground daemon startup defect omitted its loaded configuration receipt, causing the first check to replace the fixture-owned daemon. The daemon now publishes the identity of the exact parsed configuration before exposing IPC; the launcher cannot overwrite it. The cold fixture checks identity and PID ownership and closes its repository endpoint even after an unexpected replacement. The post-run process census observed zero remaining daemons after the corrected four-test run. An earlier intermittent empty plugin list lacked sufficient retained detail for a definitive causal attribution; its original failure remains recorded, rather than being declared explained merely by a subsequent pass.

The debt correction is frozen at `99172ec2`; its combination with the daemon ownership correction is `b021533d`.
